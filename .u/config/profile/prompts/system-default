# Do NOT edit this file.
#
# This script serves as a placeholder to allow the user to utilize the prompt set by their existing user or system shell initialization files, such as `~/.profile` or `~/.bashrc`; rather than using the prompt hot-swapping system to set the prompt.